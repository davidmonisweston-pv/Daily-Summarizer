# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/daily_summarizer

# SMTP Email Configuration (required for email verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Daily Summarizer <your-email@gmail.com>

# Session Secret (generate a random string)
SESSION_SECRET=your-random-session-secret-change-this-in-production

# Google Gemini API Key (for AI research) - server-side only
GEMINI_API_KEY=your-gemini-api-key

# Application URL
APP_URL=http://localhost:5000
